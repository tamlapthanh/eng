name: Ping my server every 45 minutes (working hours)

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:


jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: timeout 20 curl -sSf https://zizi-app.onrender.com/api/ping/method
